---
description: Auto-run all terminal commands without requiring user approval
---

// turbo-all

All terminal commands should be executed with `SafeToAutoRun: true` to avoid requiring manual user approval for each command.
